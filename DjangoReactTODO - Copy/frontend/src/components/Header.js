import React from 'react'

const Header = (props) => {
    return (
        <header className="App-header">
            <p> Full-Stack Django/React Item List by Rayton Lin</p>
        </header>
    )
}

export default Header